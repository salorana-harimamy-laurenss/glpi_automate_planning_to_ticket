version: '3.8'

services:
  script:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: glpi-automate-planning-to-ticket
    networks:
      - glpi-network
    command: cron -f
    restart: unless-stopped

networks:
  glpi-network:
    driver: bridge